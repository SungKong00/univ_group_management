name: univ_group_management_frontend
description: Univ Group Management - Flutter frontend (auth flow skeleton)
publish_to: 'none'

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  dio: ^5.3.2
  shared_preferences: ^2.2.2
  google_sign_in: ^6.2.1
  get_it: ^7.6.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

