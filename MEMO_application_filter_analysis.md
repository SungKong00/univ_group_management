# 가입 신청 및 모집 공고 필터 분석 (Application Filter Analysis)

## 작성일: 2025-10-24

---

## 1. 현황 조사 결과

### 1.1 가입 신청 (Join Request)

#### Provider 구조
- **파일**: `lib/presentation/pages/member_management/providers/join_request_provider.dart`
- **현재 상태**: ❌ **필터링 기능 없음**
- **구조**:
  - `joinRequestListProvider`: 단순 전체 목록 조회 (`getPendingRequests`)
  - `approveJoinRequestProvider`: 승인 처리
  - `rejectJoinRequestProvider`: 거절 처리

#### UI 구조
- **파일**: `lib/presentation/pages/member_management/widgets/join_request_section.dart`
- **현재 기능**:
  - ✅ 가입 신청 목록 표시 (카드 형태)
  - ✅ 승인/거절 버튼
  - ❌ 필터 UI 없음
  - ❌ 상태별 필터 (PENDING/APPROVED/REJECTED) 없음
  - ❌ 날짜 필터 없음

#### 데이터 모델
- **파일**: `lib/core/models/member_models.dart`
- **JoinRequest 모델**:
  ```dart
  class JoinRequest {
    final int id;
    final String userId;
    final String userName;
    final String email;
    final String? profileImageUrl;
    final String message; // 지원 동기
    final DateTime requestedAt;
    final JoinRequestStatus status; // PENDING, APPROVED, REJECTED
  }
  ```

#### 필터링 가능성
- **상태별**: `JoinRequestStatus` (PENDING/APPROVED/REJECTED)
- **날짜별**: `requestedAt` 기준 (최근 7일, 30일 등)
- **키워드**: `message` (지원 동기), `userName`, `email` 검색

---

### 1.2 모집 공고 지원서 (Recruitment Application)

#### Provider 구조
- **파일**:
  - `lib/presentation/pages/member_management/providers/recruitment_application_provider.dart`
  - `lib/presentation/providers/recruitment_providers.dart`
- **현재 상태**: ❌ **필터링 기능 없음**
- **구조**:
  - `activeRecruitmentProvider`: 활성 모집 공고 조회
  - `recruitmentApplicationsProvider`: 지원자 전체 목록 조회
  - `reviewApplicationProvider`: 지원서 심사 (승인/거절)

#### UI 구조
- **파일**: `lib/presentation/pages/member_management/widgets/recruitment_application_section.dart`
- **현재 기능**:
  - ✅ 활성 모집 공고 확인
  - ✅ 지원자 목록 표시 (카드 형태)
  - ✅ 상태 배지 표시 (대기 중/승인됨/거절됨/철회됨)
  - ✅ 승인/거절 버튼 (PENDING 상태만 활성화)
  - ❌ 필터 UI 없음
  - ❌ 상태별 필터 없음
  - ❌ 날짜 필터 없음

#### 데이터 모델
- **파일**: `lib/core/models/recruitment_models.dart`
- **ApplicationSummaryResponse** (목록용):
  ```dart
  class ApplicationSummaryResponse {
    final int id;
    final UserSummaryResponse applicant;
    final String? motivation;
    final ApplicationStatus status; // pending, approved, rejected, withdrawn
    final DateTime appliedAt;
  }
  ```
- **ApplicationResponse** (상세용):
  ```dart
  class ApplicationResponse {
    final int id;
    final RecruitmentSummaryResponse recruitment;
    final UserSummaryResponse applicant;
    final String? motivation;
    final Map<int, String> questionAnswers; // 추가 질문 답변
    final ApplicationStatus status;
    final UserSummaryResponse? reviewedBy;
    final DateTime? reviewedAt;
    final String? reviewComment;
    final DateTime appliedAt;
    final DateTime updatedAt;
  }
  ```

#### 필터링 가능성
- **상태별**: `ApplicationStatus` (pending/approved/rejected/withdrawn)
- **날짜별**: `appliedAt` 기준 (최근 7일, 30일 등)
- **키워드**: `motivation` (지원 동기), `applicant.name`, `applicant.email` 검색
- **심사자별**: `reviewedBy` (누가 승인/거절했는지)

---

## 2. 비교 분석

### 2.1 공통점
| 항목 | 가입 신청 | 모집 공고 지원서 |
|------|----------|----------------|
| 필터 존재 여부 | ❌ 없음 | ❌ 없음 |
| 상태 기반 필터 가능성 | ✅ 3가지 (PENDING/APPROVED/REJECTED) | ✅ 4가지 (pending/approved/rejected/withdrawn) |
| 날짜 필터 가능성 | ✅ requestedAt | ✅ appliedAt |
| UI 구조 | 카드 리스트 | 카드 리스트 |
| 승인/거절 기능 | ✅ 있음 | ✅ 있음 |

### 2.2 차이점

#### 가입 신청 (Join Request)
- **특징**:
  - ✅ **단순한 데이터 구조** (7개 필드)
  - ✅ **1:1 관계** (그룹 - 신청자)
  - ✅ **빠른 구현 가능** (필터 기준 적음)
  - ❌ **사용 빈도 낮음** (직접 가입 신청 방식)

#### 모집 공고 지원서 (Recruitment Application)
- **특징**:
  - ✅ **복잡한 데이터 구조** (10개 필드 + 추가 질문 답변)
  - ✅ **1:N:M 관계** (그룹 - 모집 공고 - 지원자들)
  - ✅ **높은 사용 빈도** (모집 시스템이 주 사용 방식)
  - ✅ **더 많은 필터 기준** (상태, 날짜, 심사자, 키워드)
  - ❌ **구현 복잡도 높음** (활성 모집 공고 확인 필요)

---

## 3. 추천 의견

### 🎯 우선순위: **모집 공고 지원서 (Recruitment Application)**

#### 추천 근거
1. **사용 빈도**:
   - 모집 시스템이 주 사용 방식 (docs/concepts/recruitment-system.md)
   - 가입 신청은 보조 기능

2. **필터링 필요성**:
   - 모집 공고는 수십~수백 명의 지원자 관리 가능성
   - 상태별 필터 (대기/승인/거절/철회) 필수
   - 날짜별 필터로 최근 지원자 우선 확인

3. **비즈니스 가치**:
   - 운영진이 지원자를 효율적으로 심사할 수 있음
   - 심사 완료된 지원자와 대기 중인 지원자 분리 조회
   - 통계 분석 기능으로 확장 가능 (승인율, 거절율 등)

4. **Generic Filter 패턴 적합성**:
   - MemberFilter와 유사한 구조
   - FilterModel 인터페이스 구현 용이
   - GenericFilterNotifier 재사용 가능

---

## 4. 모집 공고 지원서 필터 구현 계획

### 4.1 필터 기준 (ApplicationFilter)

```dart
class ApplicationFilter implements FilterModel {
  final List<ApplicationStatus>? statuses;  // 상태별: pending, approved, rejected, withdrawn
  final DateTime? startDate;                // 지원 시작일
  final DateTime? endDate;                  // 지원 종료일
  final String? keyword;                    // 키워드: 이름, 이메일, 지원 동기
  final int? reviewedById;                  // 심사자 ID (선택)
}
```

### 4.2 UI 구성

#### 필터 패널 (ApplicationFilterPanel)
```
┌─────────────────────────────────────────────┐
│ [상태별 필터]                                │
│ ○ 전체  ○ 대기 중  ○ 승인됨  ○ 거절됨  ○ 철회됨 │
├─────────────────────────────────────────────┤
│ [날짜 필터]                                  │
│ ○ 전체  ○ 최근 7일  ○ 최근 30일  ○ 기간 선택   │
├─────────────────────────────────────────────┤
│ [키워드 검색]                                │
│ 🔍 이름, 이메일, 지원 동기 검색               │
└─────────────────────────────────────────────┘
```

#### 통계 요약 (선택)
```
┌─────────────────────────────────────────────┐
│ 전체: 50명 | 대기: 20명 | 승인: 25명 | 거절: 5명 │
└─────────────────────────────────────────────┘
```

### 4.3 구현 파일 구조

```
lib/
├── core/
│   ├── models/
│   │   └── application_filter.dart (신규)
│   └── providers/
│       └── application/
│           ├── application_filter_provider.dart (신규)
│           └── filtered_application_list_provider.dart (신규)
└── presentation/
    └── pages/
        └── member_management/
            └── widgets/
                ├── recruitment_application_section.dart (수정)
                └── application_filter_panel.dart (신규)
```

### 4.4 예상 코드 감소량

- **MemberFilter 사례**: 38줄 감소, 버그 수정
- **ApplicationFilter 예상**: **40~50줄 감소** (유사한 구조)
  - 기존 상태 관리 로직 제거
  - Generic 패턴으로 통합
  - 필터 초기화 로직 단순화

### 4.5 개선 효과

#### 기능적 개선
- ✅ 상태별 필터 (대기/승인/거절/철회)
- ✅ 날짜별 필터 (최근 7일, 30일, 기간 선택)
- ✅ 키워드 검색 (이름, 이메일, 지원 동기)
- ✅ 필터 초기화 버튼
- ✅ 필터 개수 배지

#### 코드 품질 개선
- ✅ Generic Filter 패턴 재사용
- ✅ 일관된 필터링 로직
- ✅ 테스트 용이성 증가
- ✅ 유지보수성 향상

---

## 5. 가입 신청 필터 (향후 고려)

가입 신청 필터는 **Phase 2**로 연기하는 것을 추천합니다.

### 이유
1. **사용 빈도 낮음**: 직접 가입 신청보다 모집 시스템이 주 사용 방식
2. **필터 필요성 낮음**: 가입 신청은 소수 (일반적으로 5~10명 이하)
3. **단순한 구조**: 상태별 필터만으로도 충분

### 향후 구현 시 고려사항
- **JoinRequestFilter**: ApplicationFilter와 거의 동일한 구조
- **구현 시간**: 2~3시간 (ApplicationFilter 경험 바탕)
- **재사용**: GenericFilterNotifier 그대로 사용 가능

---

## 6. 구현 우선순위 요약

### ✅ Phase 1: 모집 공고 지원서 필터 (추천)
- **목표**: 지원자 관리 효율화
- **소요 시간**: 6~8시간
- **예상 효과**: 40~50줄 감소, 기능 대폭 개선

### ⏳ Phase 2: 가입 신청 필터 (선택)
- **목표**: 일관된 필터 경험 제공
- **소요 시간**: 2~3시간
- **예상 효과**: 20~30줄 감소

---

## 7. 백엔드 API 확인 필요 사항

### 현재 API
```
GET /api/recruitments/{recruitmentId}/applications
```

### 필터링 지원 필요
```
GET /api/recruitments/{recruitmentId}/applications
  ?status=PENDING,APPROVED
  &startDate=2025-10-01
  &endDate=2025-10-31
  &keyword=김철수
```

**확인 필요**: 백엔드에서 필터링 쿼리 파라미터 지원 여부
- 지원하지 않으면 backend-architect에게 요청 필요
- 클라이언트 사이드 필터링은 성능 이슈 가능성 (지원자 수 많을 경우)

---

## 8. 다음 단계

1. **사용자 확인**: 모집 공고 지원서 필터 구현에 동의하는지 확인
2. **백엔드 확인**: API 필터링 지원 여부 확인
3. **구현 시작**: ApplicationFilter 모델 및 Provider 구현
4. **UI 구현**: 필터 패널 및 통계 요약
5. **테스트**: 필터 조합 테스트 및 엣지 케이스 확인

---

**작성자**: Frontend Specialist
**검토 대상**: 사용자, Backend Architect (API 확인)
